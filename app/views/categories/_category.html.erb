
<tr id="category_<%= category.id %>">
    <td><%= category.name %></td>
    <td><%= category.products.count %></td>
    <td><%= link_to 'Remove', category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        
</tr>
