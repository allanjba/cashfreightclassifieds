<div style="margin: 50px;">
<h1>Categories</h1>
    <div class="ui list">
      <% @categories.each do |category| %>
      <a class="item" href="<%= category_path(category) %>"><%= category.name%> (<%= category.products.count %>)</a>
      <% end %>
    </div>
  </div>