<div class="linkIcon" id="notification-menu">
    <div class="ui icon top right inline item dropdown" style="margin-right: 0">
        <!-- Unread notification size -->
        <div class="notification-size red <%= "hide" if notification_unread_size < 1 %>"><%= notification_unread_size %></div>
        <i  class="bell icon"></i>
        <div class="ui list menu">
            <%= render partial: 'notifications/items/notification_item' %>
        </div>
    </div>
</div>
