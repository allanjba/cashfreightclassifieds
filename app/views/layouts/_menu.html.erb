
    <div class="menu">
        <div class="header">Menu</div>
        <!-- only if logged in -->
        <% if current_user %>
          <% if current_user.admin? %>
              <div class="item"><%= link_to 'Dashboard', dashboard_path %></div>
          <% end %>
          <div class="item"><%= link_to 'New Listing', new_product_path %></div>
          <div class="item"><%= link_to 'My Listings', my_listings_path %></div>
          <div class="item"><%= link_to 'My Watch List', my_watchlist_path %></div>
          <div class="ui divider"></div>
          <div class="item"><%= link_to 'Profile', current_user %></div>
        <% end %>
        <!-- end if logged in -->
        <div class="item">Settings</div>
        <div class="item">Help</div>
          <% if user_signed_in? %>
            <div class="item"><%= link_to('Logout', destroy_user_session_path, method: :delete) %></div>
          <% else %>
            <div class="item"><%= link_to 'Login', new_user_session_path %></div>
            <div class="item"><%= link_to 'Sign Up', new_user_registration_path %></div>
          <% end %>
        </div>
    </div>


